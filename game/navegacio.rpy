label inventory:
    $ objectes = len(items)
    if objectes == 0:
        n "No tens res"
    else:
        if "galleda" in items:
            n "Tens una galleda"
        if "diari" in items:
            n "Tens un diari"
return

    


